# Player Technology — Current Authority

This directory intentionally contains only the current TL/machine/catalog authorities plus two frozen compatibility sources. Historical TL tables/candidates are under `docs/archive/player_technology/pre-cp165-active/`.
