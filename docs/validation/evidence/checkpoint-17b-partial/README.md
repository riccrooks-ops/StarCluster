# Checkpoint 17b partial validation evidence

This folder preserves the evidence attached after the successful Checkpoint 17b apply/build/test run:

- Godot startup log;
- one debug-panel screenshot;
- four authoritative encounter `.log` / `.jsonl` pairs; and
- the annotated partial runbook under `docs/validation/archive/Checkpoint_17b_Partial_Validation_Results.md`.

The evidence established that the simulation/build foundation remained sound and that the friendly fixture functioned, but the 1280x800 debug pane was still too short and some observer-safe/manual checks remained incomplete. Checkpoint 17c addresses those presentation issues and carries the remaining checks forward.
