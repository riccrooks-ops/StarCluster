# Checkpoint 88 - Cross-TL Power-Aware Information Control and Construction Guardrails

## Objective

Strengthen the accepted CP87a cross-TL foundation without retuning gameplay values: enforce the mandatory Main Weapon/Reactor combat core, use actual-rated-cost power-aware EW doctrine, add absolute combat-viability gating, and broaden attribution of the TL1->TL2 information-control cliff.

## Required native run

1. `powershell -ExecutionPolicy Bypass -File .\tools\checkpoints\checkpoint-88\apply_checkpoint_88.ps1 -RepositoryOnly`
2. `powershell -ExecutionPolicy Bypass -File .\tools\checkpoints\checkpoint-88\apply_checkpoint_88.ps1 -Jobs 24`

Do not run Deep Calibration initially unless the normal evidence identifies a dependency-relevant reason.

## Required contracts

- CP87a native provenance and frozen unrelated production code remain intact.
- Concept v0.6y adds only the durable minimum combat-core construction rule.
- Cross-TL schema v2 requires >=1 Main Weapon and >=1 Reactor while preserving optional explicit duplication and allowing power-overcommitted legal builds.
- 512 legal builds, 29 named recipes, 96 ordered logical pairings, and 288 generated variants.
- Generated study binds the accepted preserve-combat-package AI doctrine and actual rating-scaled ECM/ECCM affordability.
- Actual-consumer preflight and 288-trial smoke precede 2,880,000 substantive trials.
- Intended-active fixed references with attack opportunities must actually attack; dynamic contexts must preserve attack types active in the healthy fixed reference.
- No automatic information-control, weapon, defense, or power retuning/promotion.
