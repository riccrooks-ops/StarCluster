# Checkpoint 42 - Within-Band Progression and TL2 Identity Preservation

## Objective

Validate the revised progression principle and promote no production TL2 values yet. Checkpoint 42 centers low-tech within-band advancement near 60/40, retains the former 66.7-percent package as an aggressive control, adds ordered cross-family analytical calibration, enforces weapon-identity diagnostics, and runs a complete minimal-tactics TL2 candidate Monte Carlo grid.

## Authoritative command

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass `
  -File .\tools\checkpoints\checkpoint-42\apply_checkpoint_42.ps1 `
  -Trials 10000 `
  -Jobs 24
```

Run from a clean full-repository extraction. Preserve the complete `out/checkpoint-42` directory.

## Required closure

- Clean manifest and PowerShell parsing.
- Clean warning-as-error .NET build.
- 800 compiled tests pass.
- All retained deterministic and calibration stages pass.
- Analytical study consumes 12 mirror plus 36 ordered TL1 calibration rows.
- Identity-Preserving Refinement is evaluated against the 57-64 percent review band.
- Aggressive Balanced Control remains available as an upper comparison.
- All three candidate profiles appear in exactly 324 new minimal-tactics variants.
- Same-TL and reciprocal TL2/TL1 coverage is complete across all ordered families and Ranges 2-5.
- Identity convergence is reported explicitly; no candidate is promoted automatically.

## Interpretation

The within-band percentage is a review target, not a hard optimization command. A healthy candidate preserves weapon-family identity, keeps all three families tactically valid, avoids universal dominance or inability to make progress, maintains acceptable pacing, and fits the TL1-3 refinement rhythm. Naming may be revised if the surviving mechanics imply a different technological promise.
