# Checkpoint 95 Validation Tiers

## Must always run

- native dependency guard, repository contract, pinned SDK, warning-as-error build, unit tests;
- deterministic/mechanics and resource regressions;
- cross-TL foundation v0.6 preflight and generation;
- generated 1,440-variant actual-consumer preflight;
- 1-trial x 1,440 full-pipeline smoke;
- 1,500-trial x 1,440 substantive causal replay; and
- 59 ScenarioRunner self-tests.

## CP95 hardening gates

- explicit generated runtime readiness class/range matches profile-label metadata;
- path closest approach never exceeds minimum observed post-Movement firing-window range;
- side/mutual ready-window counts and per-trial percentages reconcile;
- runtime action outside the precomputed structural ready-range estimate is retained as review telemetry rather than treated as a counter-integrity failure;
- structurally ready fixed references record a post-Movement ready firing window;
- dynamic variants that never reach a ready window are excluded from blocking activity cohorts and reported as movement diagnostics;
- reached mutual ready geometry with zero family-appropriate actions is blocking in substantive runs;
- one-side inactivity, path/post-Movement divergence, runtime-action/structural-readiness divergence, and mover-order sensitivity remain explicit review evidence.

## Deep Calibration

Not applicable. CP95 replays the accepted CP94 workload with identical sampling and combat seeds specifically to validate instrumentation.
