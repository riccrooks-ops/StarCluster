# Checkpoint 103 Validation Tiers

## Purpose

Checkpoint 103 is the first substantive TL1/TL2/TL3 integration/permutation measurement checkpoint after native acceptance of CP102 Corrected Replacement 3. It also establishes the approved research-simulator separation: **C#/Godot remains authoritative for game mechanics; stdlib-only CPython 3.13.x owns CP103 study validation, exhaustive build enumeration, population sampling, Monte Carlo screening, aggregation, and analysis.** CP103 changes no accepted gameplay or technology value.

## Tier 0 — repository and dependency contract

Must always run.

- exact accepted CP102 definition/manifest provenance;
- CP103 normal/deep definitions inspect their own wrapper/contract and both active definitions;
- historical no-Python behavior remains default; CP103 explicitly opts in to `python` / `python3` / `py` for the research wrapper only;
- CP103 Python runtime policy requires CPython 3.13.x and standard library only;
- the Windows PowerShell 5.1 wrapper must discover Python with `--version`, not a quoted `python -c` bootstrap probe, and RepositoryOnly must reject reintroduction of that marshalling hazard;
- checkpoint PowerShell source guards must themselves remain parser-safe: use anchored regex/source-shape checks rather than quote-heavy `.Contains(...)` literals that embed PowerShell syntax; the Python unit corpus independently rejects that CR5 failure pattern;
- v8 JSON Schema and executable Python validator require `checkpoint` to be JSON string `"103"`;
- primary and overlay study documents retain exact counts, seeds, axes, and construction guardrails;
- CP103 C# ScenarioRunner files used by the research path are frozen to their native-accepted CP102 bytes rather than extended with new CP103 study-specific routing;
- final repository manifest, stale-artifact normalization, and RepositoryOnly -> full-run sequence safety.

RepositoryOnly does not need Python installed merely to validate repository ownership. The full run does require CPython 3.13.x and gives a direct dependency message if unavailable.

## Tier 1 — native C# mechanics regression

Must always run.

- pinned .NET SDK 8.0.423;
- warning-as-error build;
- 876 xUnit tests;
- 7 deterministic moving-missile scenarios;
- TL1 Phase A/B mechanics;
- TL1 Space and Sensor/EW deterministic regressions;
- resource-endurance/resource-semantics locks;
- **70** ScenarioRunner self-tests (the accepted CP102 C# surface).

This tier remains the mechanics authority. Python research output cannot redefine these mechanics.

## Tier 2 — frozen CP99/CP102 executable regressions

Must always run.

- CP99 v0.8: 11,776 legal builds / 37,184 edges / 362 logical pairings / 724 generated variants;
- CP102 v0.9: 221,184 raw / 51,264 legal construction states;
- CP102 v1.0: 43,008 raw / 38,400 legal / 220,416 typed edges / 32 variants;
- CP102 actual-consumer preflight and 32 one-trial smoke.

No CP99/CP102 stochastic outcome is balance evidence in CP103.

## Tier 3 — Python environment, unit tests, and deterministic parity

Must always run before CP103 substantive work.

- CPython major/minor exactly 3.13; patch version and executable recorded;
- standard-library-only runtime;
- six CP103 Python unit tests;
- numeric `checkpoint: 103` negative case rejected;
- exact primary/overlay population contracts reproduced;
- all 96 primary population cells non-empty;
- no legal dual-main + dual-reactor build;
- 25 deterministic parity cases cover direct-fire accuracy/clamps, layered Shield/Armor/Hull damage and Shield Hardening, passive/Low/High sensing, TL2/TL3 ECM/ECCM power semantics and duplicate non-stacking, Reactor/Tactical Computer/STL/Armor/Hull progression, K3 0-TP fire, TL3 Energy modes, Missile Move4/navigation, Shield Hardener, and PDS readiness/fallback behavior.

## Tier 4 — CP103 primary population validation and smoke

Must always run.

- v1.1 strict executable validation: 921,600 raw / 164,160 legal;
- exact/near/underfilled = 43,584 / 82,848 / 37,728;
- 96 population cells;
- deterministic statistical sampler fills 240 base pairs in exactly 97,848 attempts;
- diversity overlay 32 base pairs;
- named diagnostics 32 logical pairings;
- 576 logical pairings / 1,152 variants;
- one trial for every 1,152 variant before substantive execution;
- zero trial exceptions and three-family coverage.

Population inference uses only statistical representatives; diversity/named samples remain zero-weight diagnostics.

## Tier 5 — CP103 all-tier legacy diagnostic validation and smoke

Must always run.

- v1.2 strict executable validation;
- declared raw axis product 1,417,176;
- 33 named recipes / 28 unique legal builds;
- 50 logical comparisons / 100 variants;
- all population-inference weights zero;
- one trial for every 100 variant before substantive execution;
- zero trial exceptions.

## Tier 6 — substantive Python research workload

Default native/local workload:

- primary: 1,152 variants x 250 = **288,000 trials**;
- zero-weight legacy overlay: 100 variants x 250 = **25,000 trials**;
- total CP103 substantive research = **313,000 trials**.

Together with the accepted CP102 32-trial regression smoke and the two CP103 one-trial full-variant smokes, default checkpoint trial accounting remains **314,284 executions**.

The Python analysis stage consumes the primary/overlay `variants.csv` outputs and writes population-weighted screening summaries. No automatic technology promotion is permitted.

## Tier 7 — design review, non-blocking

After successful acceptance, review:

- design diversity and concentration;
- Pareto/frontier domination signals;
- legacy-component stacking;
- integer Space breakpoints;
- Tactical Power opportunity costs;
- readiness/activity cliffs;
- mover-order sensitivity;
- family-specific Kinetic/Energy/Missile behavior.

The Python tactical model is a research-screening model. Its stochastic output is not a bit-for-bit substitute for the production game engine and does not override deterministic C#/Godot mechanics authority.

## Deep Calibration

There is no separate CP103 Deep Calibration workload. The deep definition is an exact workload alias.
