# Checkpoint 96 Validation Tiers

## Must always run

- native dependency guard, repository contract, pinned SDK, warning-as-error build, unit tests;
- deterministic/mechanics and resource regressions;
- exact accepted-CP95 cross-TL v0.6 preflight and generation;
- generated 1,440-variant actual-consumer preflight;
- 1-trial x 1,440 full-pipeline smoke;
- 1,500-trial x 1,440 substantive causal replay; and
- 59 ScenarioRunner self-tests.

## CP96 cohort-semantics gates

- preserve CP95 gameplay, legal-build envelope, adaptive sample, seeds, and combat stream;
- report **reference-context mutual readiness** independently from runtime activity;
- report whether an **observed post-Movement reference-ready firing window** actually occurred;
- report **runtime bilateral activity** solely from both ships producing family-appropriate legal combat actions, regardless of the static reference estimate;
- preserve the CP95 `observed_active` semantics only as the explicitly named `legacy_cp95_reference_expected_runtime_active` compatibility cohort;
- classify reference-context/runtime mismatches explicitly, including `reference_not_expected_runtime_active` and `reference_expected_runtime_inactive`;
- retain path/post-Movement, runtime-action/reference-estimate, activity, and initiative outliers as review evidence rather than automatic balance rules; and
- do not change component values, gameplay rules, technology promotion, AI doctrine, or sampling policy.

## Completion boundary

CP96 closes the current narrow instrumentation/interpretation sequence. After native acceptance and review, ordinary development should return to broader game-mechanics, mixed-TL progression, and technology-tree work. Reopen instrumentation only when a substantive later study exposes a genuine measurement blocker.

## Deep Calibration

Not applicable. CP96 replays the accepted CP95 bounded workload to validate reporting semantics, not to recalibrate gameplay.
