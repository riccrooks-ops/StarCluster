# Checkpoint 100 Validation Tiers

## Must always run

1. Native dependency/interface precheck and CP100 repository contract.
2. Root-manifest and PowerShell syntax verification.
3. Pinned SDK 8.0.423 and warning-as-error build.
4. All xUnit tests (expected 876).
5. Accepted deterministic missile, TL1 Phase A/B, construction, Sensor/EW, and resource-semantic regressions.
6. Native-accepted CP99 foundation v0.8 deterministic preflight: 11,776 legal Sensor-equipped builds, 37,184 exact TL1->TL2 progression edges, 181 strata, 362 logical pairs, and 724 generated variants.
7. Native-accepted CP99 foundation v0.8 deterministic generation regression.
8. ScenarioRunner self-tests (expected 63).

CP100 has **10 runner stages and zero stochastic trial executions**. It changes technology/design authorities and coverage registration, not executable TL3 combat mechanics.

## Deep calibration

Deep Calibration is **not applicable**. The deep alias intentionally resolves to the same bounded deterministic workload. CP99's accepted 181,000-trial exact-edge study is evidence and is not replayed merely because the technology table now registers later conceptual candidates.

## Blocking boundaries

Repository/build/test/schema/catalog/referential-integrity failures are blocking. The initial TL3 candidate values must agree across Concept, Matrix, workbook, catalogs, and the machine profile. Suite v0.15 must register the TL3 transition types while keeping CP99 foundation v0.8 byte-identical and `tl3CombatConsumerEnabled=false`. Reactor miniaturization must retain Space delta -1; Shield Hardener must remain an optional unlock; no TL3 weapon/PDS/STL/FTL placeholder progression may be invented. Candidate balance and final production promotion remain human design decisions.
